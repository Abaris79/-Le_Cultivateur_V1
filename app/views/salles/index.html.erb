<h1>Vos Oasis de Culture</h1>

<% @salles.each do |salle| %>
    <h5><%= link_to salle.name, salle_path(salle) %></h5>
  <% end %>

<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>

<h3><%= link_to "Dévoiler un Oasis", new_salle_path %></h3>
